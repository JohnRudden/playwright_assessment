{
  "scripts": {
    "test": "npx bddgen && npx playwright test --workers=4",
    "posttest": "npx http-server ./cucumber-report -c-1 -a localhost -o index.html"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^22.13.9",
    "http-server": "^14.1.1",
    "playwright-bdd": "^8.2.1"
  }
}
